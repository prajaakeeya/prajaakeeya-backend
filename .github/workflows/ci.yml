name: CI

on:
  # PRs are validated here. Pushes to main/staging are validated by the
  # `validate` gate inside deploy.yml (which the deploy jobs `needs:`), so we
  # don't also run CI on push — that would double-validate every push.
  pull_request:
    branches:
      - main
      - staging

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate (lint, typecheck, test, build)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Detect ESLint config
        id: eslint_config
        run: |
          if [ -f .eslintrc ] || [ -f .eslintrc.js ] || [ -f .eslintrc.cjs ] || [ -f .eslintrc.json ] || [ -f .eslintrc.yaml ] || [ -f .eslintrc.yml ] || [ -f eslint.config.js ] || [ -f eslint.config.cjs ] || [ -f eslint.config.mjs ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Lint
        if: steps.eslint_config.outputs.present == 'true'
        run: npm run lint:check

      - name: Lint skipped (no ESLint config)
        if: steps.eslint_config.outputs.present != 'true'
        run: echo "Skipping lint because no ESLint config file exists in the repository."

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test -- --runInBand

      - name: E2E test (HTTP-level, no DB)
        run: npm run test:e2e

      - name: Build
        run: npm run build

  # Dependency CVE scan — surfaces known vulnerabilities on every PR. Marked
  # continue-on-error so transitive advisories (mostly in build tooling) stay
  # VISIBLE without blocking merges; Dependabot opens the remediation PRs.
  security-audit:
    name: Dependency audit (non-blocking)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit production dependencies (high+)
        run: npm audit --omit=dev --audit-level=high

      - name: Full audit report (all deps, informational)
        if: always()
        run: npm audit || true
